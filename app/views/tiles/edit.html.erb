<h1>Editing tile</h1>

<%= render "form", tile: @tile %>

<div class="actions">
  <%= button_tag "Update", form: "form" %>
  <%= button_to "Destroy", [@lobby, @tile], method: :delete %>
  <%= button_to "Cancel", edit_lobby_path(@lobby), method: :get %>
</div>
