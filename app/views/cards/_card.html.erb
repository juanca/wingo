<div id="<%= dom_id card %>">
  <p>
    <strong>Lobby:</strong>
    <%= link_to card.lobby.title, lobby_path(card.lobby) %>
  </p>

  <p>
    <strong>Player:</strong>
    <%= card.title %>
  </p>

  <%= render "tiles", tiles: card.tiles %>
</div>
